# Credit Card Default Prediction Notebook

This project contains the notebook required to create and execute a model that can predict a client's credit card default rate.
The required dependencies are declared in the pyproject.toml file.

AI (Codex) was used to generate most of the code. 
